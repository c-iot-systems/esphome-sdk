# Fixture: exercises the hds_v1_0 slot pin table BY NAME — one substitution per module type the board
# supports, wired into a gpio pin. A typo'd slot name stays a ${...} literal that the pin schema
# rejects, so `esphome config` fails; that is what makes the names exercised, not merely declared.
# ignore_pin_validation_error is set because some legitimate slot pins are flash/strapping pins that
# ESPHome's generic ESP32 check would reject. It does NOT hide a mistyped name.
substitutions:
  device_name: hds-v1-0-slots
  firmware_version: "0.0.0-test"
  wifi_ssid: test-ssid
  wifi_password: test-wifi-password
  wifi_ap_password: test-ap-password

binary_sensor:
  - platform: gpio
    pin:
      number: ${slot_1_triple_digital_input_in1}
      ignore_pin_validation_error: true
    name: "slot_1_triple_digital_input_in1"
    internal: true
  - platform: gpio
    pin:
      number: ${slot_1_level_shifter_io2}
      ignore_pin_validation_error: true
    name: "slot_1_level_shifter_io2"
    internal: true
  - platform: gpio
    pin:
      number: ${slot_1_audio_tx_dat}
      ignore_pin_validation_error: true
    name: "slot_1_audio_tx_dat"
    internal: true
  - platform: gpio
    pin:
      number: ${slot_1_io_expander_keyscan_5x5_scl}
      ignore_pin_validation_error: true
    name: "slot_1_io_expander_keyscan_5x5_scl"
    internal: true
  - platform: gpio
    pin:
      number: ${slot_3_double_relay_relay_1}
      ignore_pin_validation_error: true
    name: "slot_3_double_relay_relay_1"
    internal: true
  - platform: gpio
    pin:
      number: ${slot_3_h_bridge_out_2}
      ignore_pin_validation_error: true
    name: "slot_3_h_bridge_out_2"
    internal: true
  - platform: gpio
    pin:
      number: ${slot_4_io_expander_keyscan_10x8_sda}
      ignore_pin_validation_error: true
    name: "slot_4_io_expander_keyscan_10x8_sda"
    internal: true

packages:
  criotive:
    url: https://github.com/c-iot-systems/esphome-sdk
    ref: __SDK_REF__
    files:
      - path: hardware/hds_v1_0.yaml
      - path: modules/core.yaml
        vars: {sdk_ref: __SDK_REF__}
      - path: modules/wifi.yaml
        vars: {sdk_ref: __SDK_REF__}
