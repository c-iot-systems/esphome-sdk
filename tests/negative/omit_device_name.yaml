# NEGATIVE — MUST FAIL. Omits device_name (required by core.yaml); every other required
# substitution is present. Proves core.yaml gives device_name no default.
substitutions:
  # device_name intentionally omitted
  firmware_version: "0.0.0-test"
  wifi_ssid: test-ssid
  wifi_password: test-wifi-password
  wifi_ap_password: test-ap-password

esp32:
  board: esp32dev
  framework:
    type: arduino

packages:
  core: !include ../../modules/core.yaml
  wifi: !include ../../modules/wifi.yaml
