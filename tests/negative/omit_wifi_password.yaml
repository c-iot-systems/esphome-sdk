# NEGATIVE — MUST FAIL. Omits wifi_password (required by wifi.yaml); every other required
# substitution is present. Proves wifi.yaml gives wifi_password no default (no credential default).
substitutions:
  device_name: neg-device
  firmware_version: "0.0.0-test"
  wifi_ssid: test-ssid
  # wifi_password intentionally omitted
  wifi_ap_password: test-ap-password

esp32:
  board: esp32dev
  framework:
    type: arduino

packages:
  core: !include { file: ../../modules/core.yaml, vars: {sdk_ref: local-test} }
  wifi: !include { file: ../../modules/wifi.yaml, vars: {sdk_ref: local-test} }
