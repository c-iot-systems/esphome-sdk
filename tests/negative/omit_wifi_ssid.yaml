# NEGATIVE — MUST FAIL. Omits wifi_ssid (required by wifi.yaml); every other required
# substitution is present. Proves wifi.yaml gives wifi_ssid no default.
substitutions:
  device_name: neg-device
  firmware_version: "0.0.0-test"
  # wifi_ssid intentionally omitted
  wifi_password: test-wifi-password
  wifi_ap_password: test-ap-password

esp32:
  board: esp32dev
  framework:
    type: arduino

packages:
  core: !include ../../modules/core.yaml
  wifi: !include ../../modules/wifi.yaml
