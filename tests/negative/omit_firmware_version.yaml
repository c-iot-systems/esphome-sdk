# NEGATIVE — MUST FAIL. Omits firmware_version (required by core.yaml); every other required
# substitution is present. Proves core.yaml gives firmware_version no default.
substitutions:
  device_name: neg-device
  # firmware_version intentionally omitted
  wifi_ssid: test-ssid
  wifi_password: test-wifi-password
  wifi_ap_password: test-ap-password

esp32:
  board: esp32dev
  framework:
    type: arduino

packages:
  core: !include ../../modules/core.yaml
  wifi: !include ../../modules/wifi.yaml
